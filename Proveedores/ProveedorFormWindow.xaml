<Window x:Class="POS.Proveedores.ProveedorFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:POS.Proveedores"
        mc:Ignorable="d"
        Title="Proveedor" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Nombre:" Grid.Row="0"/>
        <TextBox x:Name="txtNombre" Grid.Row="1" Margin="0,0,0,10"/>

        <Label Content="Contacto:" Grid.Row="2"/>
        <TextBox x:Name="txtContacto" Grid.Row="3" Margin="0,0,0,10"/>

        <Label Content="Teléfono:" Grid.Row="4"/>
        <TextBox x:Name="txtTelefono" Grid.Row="5" Margin="0,0,0,10"/>

        <Label Content="Email:" Grid.Row="6"/>
        <TextBox x:Name="txtEmail" Grid.Row="7" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" Grid.Row="8" HorizontalAlignment="Right">
            <Button Content="Aceptar" Width="80" Margin="0,0,10,0" Click="BtnAceptar_Click"/>
            <Button Content="Cancelar" Width="80" Click="BtnCancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>