<Page x:Class="POS.Views.Modulos.ClientesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent" Title="Clientes">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardPanel}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Style="{StaticResource DarkTextBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,12,0"/>
                <Button Grid.Column="1" Style="{StaticResource BtnPrimary}"
                        Content="&#xE109; Nuevo" FontFamily="Segoe MDL2 Assets"
                        Padding="14,8" Margin="0,0,8,0"
                        Command="{Binding NuevoCommand}"/>
                <Button Grid.Column="2" Style="{StaticResource BtnSecondary}"
                        Content="&#xE70F; Editar" FontFamily="Segoe MDL2 Assets"
                        Padding="14,8" Margin="0,0,8,0"
                        Command="{Binding EditarCommand}"/>
                <Button Grid.Column="3" Style="{StaticResource BtnDanger}"
                        Content="&#xE74D; Eliminar" FontFamily="Segoe MDL2 Assets"
                        Padding="14,8"
                        Command="{Binding EliminarCommand}"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="0">
            <DataGrid ItemsSource="{Binding Clientes}"
                      SelectedItem="{Binding SelectedCliente}"
                      Style="{StaticResource DarkDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"       Binding="{Binding IdCliente}"      Width="55"/>
                    <DataGridTextColumn Header="Nombre"   Binding="{Binding NombreCompleto}" Width="*"/>
                    <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}"       Width="120"/>
                    <DataGridTextColumn Header="Correo"   Binding="{Binding Correo}"         Width="180"/>
                    <DataGridTextColumn Header="Registro" Binding="{Binding FechaRegistro, StringFormat=dd/MM/yyyy}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
